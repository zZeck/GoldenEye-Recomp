# --- Build output ---
/out/
/build/
/cmake-build-*/
/dist/

# --- Release signing (LOCAL ONLY - never commit; back these up separately) ---
*.jks
*.keystore
android/keystore.properties
*.pdb
*.exe
*.ilk
*.exp
*.lib
*.obj
*.o

# --- Recompiled game code (GENERATED from your own copy of the game) ---
# Produced by `rexglue codegen` from the manifest + your XEX. Not committed:
# it is derived from copyrighted game code. Regenerate it locally — see README.
/generated/

# --- Game files: provide your OWN legally-owned copy of GoldenEye 007 ---
# (the XEX + extracted assets go in assets/). Never committed.
/assets/*
!/assets/.gitkeep

# --- Runtime config (written by the game at runtime) ---
ge.toml
/logs/

# --- Local dev / debug tooling (not part of the source) ---
/parse_dump.py
/sample_stack.py
/walk_thread.py
/launch_online_2p.bat
/launch_online_2p.ps1
/CODEGEN COMMAND.txt
/backtrace.txt

# --- Editor / OS ---
.vs/
.vscode/
.idea/
*.user
Thumbs.db
.DS_Store

# --- Local tooling (not for distribution) ---
/.codegraph/
/.cursor/

# --- Android build (Gradle/AGP/CMake outputs + machine-local config) ---
android/.gradle/
android/build/
android/app/build/
android/app/.cxx/
android/local.properties

# --- Local release packaging (not committed) ---
/release/

# --- Reference: FSR fork source code (not part of this project) ---
/GoldenEye-Recomp-FSR-fork/

.opencode/
